acp_team.api.teams:
  path: '/acp-team/api/teams'
  defaults:
    _controller: '\Drupal\acp_team\Controller\TeamApiController::listTeams'
    _title: 'ACP Team API'
  requirements:
    _access: 'TRUE'        # ← instead of a permission
